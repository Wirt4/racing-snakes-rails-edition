basic dev cheats for typescript and npm

to install the modules from package.json (happens more often than you think)
```bash
npm install
```

compile:
```bash
tsc
```
or ```bash
tsc -w
```
to compile and watch for changes
