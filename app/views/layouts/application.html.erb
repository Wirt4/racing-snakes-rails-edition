<!DOCTYPE html>
<html>
  <head>
    <title>"Lite" Cycles</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= tag.link rel: "stylesheet", href: "https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" %>

  </head>
  <body class="bg-black text-pink-500 font-arcade">
    <!-- notice and alerts for session status from Devise-->
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <!-- thumbnail logout/login link-->
    <!-- if the user is logged in, offer them a logout link-->
    <%if user_signed_in?%>
      <%= link_to "Log Out", destroy_user_session_path, data: {turbo_method: :delete} %>
    <% else %>
      <!-- if the user is not logged in, offer them a login/register link-->
      <%= link_to "Log In/Register", new_user_session_path %>
    <% end %>
    <%= yield %>
       </body>
</html>
